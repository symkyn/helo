const initialState = {
    username: '',
    password: '',
    profile_pic: ''
}


function reducer(state=initialState, action) {
    switch(action.type){
        default: return state;
    }
}

export default reducer;